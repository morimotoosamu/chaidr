% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_plot.R
\name{plot.chaid}
\alias{plot.chaid}
\title{Plot a CHAID tree with base graphics}
\usage{
\method{plot}{chaid}(
  x,
  cex = 0.8,
  label_len = 22,
  palette = NULL,
  show_bar = TRUE,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{cex}{Base character expansion for node and edge labels.}

\item{label_len}{Maximum number of characters for edge (split group)
labels; longer labels are truncated.}

\item{palette}{Vector of class colours for categorical responses.
Defaults to \code{grDevices::hcl.colors(n, "Dark 3")}.}

\item{show_bar}{Logical. Draw a class distribution bar inside each
node (categorical responses only).}

\item{main}{Plot title. Defaults to the response name and method.}

\item{...}{Ignored.}
}
\value{
The fitted object, invisibly.
}
\description{
Draws the tree top-down using base graphics only. For categorical
responses each node shows the predicted class, its share, the node
size and optionally a horizontal bar of the class distribution.
Edges are labelled with the (possibly merged) predictor categories.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
plot(fit)
}
\seealso{
\code{\link[=chaid]{chaid()}}, \code{\link[=chaid_dot]{chaid_dot()}} for publication-quality Graphviz
output, \code{\link[=chaid_plotly]{chaid_plotly()}} for an interactive version.
}
