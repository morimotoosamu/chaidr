% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_plotly.R
\name{chaid_plotly}
\alias{chaid_plotly}
\title{Interactive CHAID tree plot with plotly}
\usage{
chaid_plotly(fit, palette = NULL, label_len = 20, ...)
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{palette}{Vector of class colours for categorical responses
(default \code{grDevices::hcl.colors(n, "Dark 3")}, matching
\code{\link[=plot.chaid]{plot.chaid()}}).}

\item{label_len}{Maximum number of characters for edge (split group)
labels.}

\item{...}{Ignored.}
}
\value{
A 'plotly' htmlwidget.
}
\description{
Draws the tree as an interactive 'plotly' htmlwidget with zoom, pan
and hover information (reaching rule, class distribution and split
details for each node). The widget can be embedded directly in
R Markdown documents or saved as HTML.
}
\examples{
\dontshow{if (requireNamespace("plotly", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
chaid_plotly(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot.chaid]{plot.chaid()}}, \code{\link[=chaid_dot]{chaid_dot()}}
}
