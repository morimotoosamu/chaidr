% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_partykit.R
\name{chaid_as_party}
\alias{chaid_as_party}
\alias{as.party.chaid}
\title{Convert a CHAID tree to a partykit constparty}
\usage{
chaid_as_party(x, data, weights = NULL, freq = NULL, ...)

\method{as.party}{chaid}(obj, data, weights = NULL, freq = NULL, ...)
}
\arguments{
\item{x, obj}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{data}{The data frame used to fit the tree (the chaid object
does not store the data).}

\item{weights, freq}{The case and frequency weights used in the fit,
if any. Required to reproduce the case exclusions of the fit.}

\item{...}{Ignored.}
}
\value{
A \code{partykit::constparty} object.
}
\description{
Converts a fitted \code{"chaid"} object to a
\code{partykit::constparty} object so that the 'partykit' toolbox
(\code{plot()}, \code{print()}, \code{nodeapply()}, 'ggparty', ...) can be used.
Binned continuous predictors are represented as ordered factors of
the bin interval labels, and all splits become index-type
\link[partykit:partysplit]{partykit::partysplit} objects, so missing values (\code{"<NA>"} level)
are routed explicitly and split rules display the interval labels.
}
\details{
The returned object is intended for visualisation and structural
inspection. For predictions on new data use \code{\link[=predict.chaid]{predict.chaid()}}; the
\code{predict()} method of the party object expects the converted data
representation, not the original one.
}
\examples{
\dontshow{if (requireNamespace("partykit", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
pt <- chaid_as_party(fit, data = iris)
print(pt)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=chaid]{chaid()}}, \code{\link[=predict.chaid]{predict.chaid()}}
}
