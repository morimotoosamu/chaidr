% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_gains.R
\name{chaid_gains}
\alias{chaid_gains}
\alias{print.chaid_gains}
\alias{plot.chaid_gains}
\title{Gains and lift table for a CHAID tree}
\usage{
chaid_gains(fit, data = NULL, target = NULL, weights = NULL, freq = NULL)

\method{print}{chaid_gains}(x, ...)

\method{plot}{chaid_gains}(x, type = c("gains", "lift"), ...)
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{data}{Optional data frame. If \code{NULL} (default), node statistics
from training are used. If supplied, cases are routed with
\code{\link[=predict.chaid]{predict.chaid()}} and the table is recomputed, e.g. for evaluation
on holdout data.}

\item{target}{Response level of interest for categorical responses.
For a binary response the second level is used by default; for
three or more levels \code{target} is required.}

\item{weights, freq}{Case and frequency weights for \code{data}, when
supplied.}

\item{x}{A \code{"chaid_gains"} object.}

\item{...}{For \code{plot()}, further arguments passed to
\code{\link[graphics:plot]{graphics::plot()}}; ignored by \code{print()}.}

\item{type}{\code{"gains"} (default) draws the cumulative gains curve
(percentage of cases vs. percentage of captured targets, diagonal
= random), \code{"lift"} draws the cumulative lift curve (1 = random).}
}
\value{
An object of class \code{"chaid_gains"}, a list with the gains
table (\code{table}), the \code{target} level, the response type (\code{ytype}),
the overall rate (\code{overall}) and the data \code{source} (\code{"training"}
or \code{"newdata"}). \code{print()} and \code{plot()} methods are available.
}
\description{
Sorts the terminal nodes by decreasing response rate (node mean for
continuous responses) and computes cumulative gains and lift,
corresponding to the SPSS gains table.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
g <- chaid_gains(fit, target = "virginica")
print(g)
plot(g)
plot(g, type = "lift")
}
\seealso{
\code{\link[=chaid_table]{chaid_table()}}, \code{\link[=chaid_validate]{chaid_validate()}}
}
