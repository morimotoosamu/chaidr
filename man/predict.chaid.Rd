% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_predict.R
\name{predict.chaid}
\alias{predict.chaid}
\title{Predict from a fitted CHAID tree}
\usage{
\method{predict}{chaid}(object, newdata, type = c("response", "prob", "node"), ...)
}
\arguments{
\item{object}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{newdata}{A data frame containing all predictor variables used
in the fit.}

\item{type}{\code{"response"} (default) returns predicted classes (or
means for a continuous response), \code{"prob"} returns a matrix of
class probabilities (categorical responses only), \code{"node"} returns
the terminal node id for each row.}

\item{...}{Ignored.}
}
\value{
Depending on \code{type}: a factor (or numeric vector) of
predictions, a numeric matrix of class probabilities with one
column per response level, or an integer vector of node ids.
}
\description{
Routes the rows of \code{newdata} down the tree and returns predictions.
Factor levels unseen during training, and codes that did not occur in
a node when it was split, are routed to the child with the largest
node size (for ordinal predictors, to the group with the smallest
code distance); a warning is issued when unseen levels are detected.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
predict(fit, head(iris))
predict(fit, head(iris), type = "prob")
predict(fit, head(iris), type = "node")
}
\seealso{
\code{\link[=chaid]{chaid()}}
}
