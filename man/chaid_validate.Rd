% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_validate.R
\name{chaid_validate}
\alias{chaid_validate}
\alias{print.chaid_validation}
\title{Evaluate a CHAID tree on holdout data}
\usage{
chaid_validate(fit, newdata, weights = NULL, freq = NULL)

\method{print}{chaid_validation}(x, ...)
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{newdata}{A data frame of validation cases containing the
response and all predictors.}

\item{weights, freq}{Case and frequency weights for \code{newdata}.}

\item{x}{A \code{"chaid_validation"} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"chaid_validation"}: a list with
\code{nodes} (per-node comparison data frame with \code{train_pct_n},
\code{test_pct_n}, \code{train_rate}, \code{test_rate} and \code{diff_rate}),
\code{overall} (accuracy for categorical responses; RMSE and R-squared
for continuous ones), \code{n_test} and \code{ytype}. A \code{print()} method is
available.
}
\description{
Routes \code{newdata} down the fitted tree and compares, for each terminal
node, the node share and the response rate (node mean for continuous
responses) between training and validation data. Useful to check
whether the segments replicate on new data, i.e. whether the tree is
overfitting.
}
\examples{
set.seed(1)
idx <- sample(nrow(iris), 100)
fit <- chaid(Species ~ ., data = iris[idx, ],
             control = chaid_control(min_parent = 30, min_child = 10))
chaid_validate(fit, iris[-idx, ])
}
\seealso{
\code{\link[=chaid_gains]{chaid_gains()}}, \code{\link[=predict.chaid]{predict.chaid()}}
}
