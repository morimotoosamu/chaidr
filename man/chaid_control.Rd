% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_chaid.R
\name{chaid_control}
\alias{chaid_control}
\title{Control parameters for CHAID tree growing}
\usage{
chaid_control(
  alpha_merge = 0.05,
  alpha_split = 0.05,
  alpha_split_merge = 0.05,
  resplit = FALSE,
  bonferroni = TRUE,
  stat = c("pearson", "lr"),
  exhaustive_adjust = c("spss", "biggs"),
  max_depth = 3L,
  min_parent = 100,
  min_child = 50,
  min_segment = NULL,
  n_bins = 10L,
  epsilon = 0.001,
  max_iter = 100L,
  adjust_across = c("none", "bonferroni", "holm", "hochberg", "hommel", "BH", "BY")
)
}
\arguments{
\item{alpha_merge}{Significance level for merging predictor categories.
Category pairs whose test p-value exceeds this threshold are merged.}

\item{alpha_split}{Significance level for splitting a node. A node is
split only if the best (adjusted) p-value is at most this value.}

\item{alpha_split_merge}{Significance level for re-splitting a merged
compound category. Only used when \code{resplit = TRUE}.}

\item{resplit}{Logical. If \code{TRUE}, compound categories consisting of
three or more original categories are considered for a binary
re-split during the merge step (SPSS "allow resplitting" option).}

\item{bonferroni}{Logical. If \code{TRUE} (default), split p-values are
Bonferroni-adjusted by the number of ways the predictor categories
can be merged into the final groups.}

\item{stat}{Chi-squared statistic for categorical responses:
\code{"pearson"} (default) or \code{"lr"} (likelihood ratio).}

\item{exhaustive_adjust}{Bonferroni multiplier convention used by
Exhaustive CHAID: \code{"spss"} (default) follows the IBM SPSS algorithm
document, \code{"biggs"} follows Biggs, de Ville and Suen (1991).}

\item{max_depth}{Maximum tree depth (root has depth 0).}

\item{min_parent}{Minimum number of cases (frequency-weighted) a node
must contain to be considered for splitting.}

\item{min_child}{Minimum number of cases (frequency-weighted) in each
child node.}

\item{min_segment}{Optional minimum size for a merged category group
during the merge step (SPSS algorithm step 7). Groups smaller than
this are absorbed into the most similar allowable group. \code{NULL}
(default) disables this step.}

\item{n_bins}{Number of quantile bins used to discretise continuous
predictors.}

\item{epsilon}{Convergence tolerance for the iterative estimation of
expected cell frequencies with case weights.}

\item{max_iter}{Maximum number of iterations for the same estimation.}

\item{adjust_across}{Multiple-comparison adjustment applied across
predictors within a node before comparing against \code{alpha_split}.
\code{"none"} (default, as in SPSS) or one of the \code{\link[stats:p.adjust]{stats::p.adjust()}}
methods \code{"bonferroni"}, \code{"holm"}, \code{"hochberg"}, \code{"hommel"}, \code{"BH"},
\code{"BY"}. Note that the adjustment family is the node, not the whole
tree, and that \code{"holm"} always yields the same tree as
\code{"bonferroni"} because only the minimum adjusted p-value is compared
with \code{alpha_split}.}
}
\value{
An object of class \code{"chaid_control"}: a list of the validated
parameter values, to be passed to the \code{control} argument of
\code{\link[=chaid]{chaid()}}.
}
\description{
Collects the algorithm parameters used by \code{\link[=chaid]{chaid()}}. The defaults match
the defaults of the IBM SPSS Statistics user interface (alpha 0.05,
Bonferroni adjustment on, no re-splitting, maximum depth 3, minimum
parent size 100, minimum child size 50, 10 bins for continuous
predictors).
}
\examples{
ctl <- chaid_control(max_depth = 2, min_parent = 20, min_child = 5)
fit <- chaid(Species ~ ., data = iris, control = ctl)
fit
}
\seealso{
\code{\link[=chaid]{chaid()}}
}
