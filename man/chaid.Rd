% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_chaid.R
\name{chaid}
\alias{chaid}
\title{Fit a CHAID or Exhaustive CHAID decision tree}
\usage{
chaid(
  formula,
  data,
  weights = NULL,
  freq = NULL,
  method = c("chaid", "exhaustive"),
  control = chaid_control(),
  costs = NULL,
  y_scores = NULL
)
}
\arguments{
\item{formula}{A model formula of the form \code{response ~ predictors}.}

\item{data}{A data frame containing the variables in the formula.}

\item{weights}{Optional numeric vector of case weights. They only
affect the estimation of expected cell frequencies; cases with
missing, zero or negative weights are excluded.}

\item{freq}{Optional numeric vector of frequency weights. They
determine observed counts, degrees of freedom and node sizes.
Non-integer values are rounded to the nearest integer (IBM
specification).}

\item{method}{\code{"chaid"} (default) for the Kass (1980) algorithm or
\code{"exhaustive"} for Exhaustive CHAID (Biggs, de Ville and Suen,
1991).}

\item{control}{A \code{"chaid_control"} object created by
\code{\link[=chaid_control]{chaid_control()}}.}

\item{costs}{Optional misclassification cost matrix \code{C[truth, pred]}
for categorical responses (same convention as the \code{loss} matrix of
'rpart': zero diagonal, non-negative entries, dimnames matching the
response levels). When supplied, node predictions minimise expected
cost instead of taking the majority class. As in SPSS, costs do not
affect tree growing or the significance tests.}

\item{y_scores}{Optional numeric vector of class scores for an ordinal
response, in the order of \code{levels(y)}. Defaults to the class ranks
\verb{1..J}. Fixed at the start of tree growing and not re-ranked in
subtables (IBM specification).}
}
\value{
An object of class \code{"chaid"}: a list with components \code{call},
\code{method}, \code{control}, \code{response} (name, type, levels, scores),
\code{predictors} (internal coding of each predictor), \code{nodes} (list of
node records with distribution, prediction and split information),
\code{costs}, \code{n} (number of cases used) and \code{n_dropped} (number of
excluded cases).
}
\description{
Grows a CHAID (Chi-squared Automatic Interaction Detection) or
Exhaustive CHAID decision tree following the IBM SPSS Statistics
algorithm specification. Nominal (\code{factor}), ordinal (\code{ordered}) and
continuous (\code{numeric}) predictors are supported; continuous predictors
are discretised into quantile bins first. The response may be nominal
(Pearson or likelihood-ratio chi-squared test), ordinal (Goodman row
effects test) or continuous (one-way ANOVA F test).
}
\details{
Missing predictor values are handled as in SPSS: for ordinal
predictors they form a floating category that may merge with any
group, for nominal predictors they form an ordinary extra category.
Cases with a missing response, missing/zero/negative weights, or all
predictors missing are dropped before fitting.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
print(fit)
predict(fit, head(iris))
}
\references{
Kass, G. V. (1980). An exploratory technique for investigating large
quantities of categorical data. \emph{Applied Statistics}, 29(2), 119-127.

Biggs, D., de Ville, B., & Suen, E. (1991). A method of choosing
multiway partitions for classification and decision trees. \emph{Journal
of Applied Statistics}, 18(1), 49-62.
}
\seealso{
\code{\link[=chaid_control]{chaid_control()}}, \code{\link[=predict.chaid]{predict.chaid()}}, \code{\link[=chaid_table]{chaid_table()}},
\code{\link[=chaid_rules]{chaid_rules()}}, \code{\link[=plot.chaid]{plot.chaid()}}
}
