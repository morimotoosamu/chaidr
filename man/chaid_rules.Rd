% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_inspect.R
\name{chaid_rules}
\alias{chaid_rules}
\title{Extract decision rules from a CHAID tree}
\usage{
chaid_rules(fit, nodes = NULL, format = c("text", "sql", "r"))
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{nodes}{Integer vector of node ids. Defaults to all terminal
nodes.}

\item{format}{\code{"text"} (default) for human-readable conditions,
\code{"sql"} for SQL \code{WHERE} clauses, or \code{"r"} for R logical
expressions that reproduce the node assignment exactly when
evaluated against the data.}
}
\value{
A data frame with columns \code{node} (integer id) and \code{rule}
(character).
}
\description{
Returns the condition that a case must satisfy to reach each terminal
node (or each of the requested nodes) as a rule string.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
chaid_rules(fit)
chaid_rules(fit, format = "sql")
}
\seealso{
\code{\link[=chaid]{chaid()}}, \code{\link[=chaid_table]{chaid_table()}}
}
