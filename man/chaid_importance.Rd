% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_inspect.R
\name{chaid_importance}
\alias{chaid_importance}
\title{Heuristic variable importance for a CHAID tree}
\usage{
chaid_importance(fit)
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}
}
\value{
A data frame with one row per predictor actually used for a
split, sorted by decreasing importance: \code{variable}, \code{n_splits},
\code{min_p_adj}, \code{importance} and \code{importance_pct} (share of the total
importance in percent).
}
\description{
SPSS defines no official importance measure for CHAID, and the
chi-squared and F statistics of different splits are not directly
comparable because their degrees of freedom differ. This function
therefore uses a p-value based heuristic: for each predictor,
\code{importance} is the sum over its splits of
\verb{(node size / root size) * (-log10(adjusted p-value))}, i.e. a
variable is important when it splits large nodes with strong
significance.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
chaid_importance(fit)
}
\seealso{
\code{\link[=chaid]{chaid()}}, \code{\link[=chaid_table]{chaid_table()}}
}
