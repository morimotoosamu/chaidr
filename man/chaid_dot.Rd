% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_graphviz.R
\name{chaid_dot}
\alias{chaid_dot}
\alias{print.chaid_dot}
\alias{chaid_graphviz}
\title{Render a CHAID tree as Graphviz DOT}
\usage{
chaid_dot(
  fit,
  palette = NULL,
  rankdir = "TB",
  label_len = 28,
  legend = TRUE,
  file = NULL
)

\method{print}{chaid_dot}(x, ...)

chaid_graphviz(fit, ...)
}
\arguments{
\item{fit}{A fitted \code{"chaid"} object returned by \code{\link[=chaid]{chaid()}}.}

\item{palette}{Vector of class colours for categorical responses
(default \code{grDevices::hcl.colors(n, "Dark 3")}, matching
\code{\link[=plot.chaid]{plot.chaid()}}). For continuous responses node fills use a
white-to-steelblue gradient of the node means.}

\item{rankdir}{Graph direction: \code{"TB"} (top to bottom, default) or
\code{"LR"} (left to right).}

\item{label_len}{Maximum number of characters for edge (split group)
labels.}

\item{legend}{Logical. Add a legend node with the class colours
(categorical responses only).}

\item{file}{Optional path; when given, the DOT source is also
written there in UTF-8 for use with the external \code{dot} command.}

\item{x}{A \code{"chaid_dot"} object.}

\item{...}{For \code{chaid_graphviz()}, arguments passed on to
\code{chaid_dot()}; ignored by \code{print()}.}
}
\value{
For \code{chaid_dot()}, the DOT source as a character string of
class \code{"chaid_dot"} (returned invisibly; its \code{print()} method
outputs the source). For \code{chaid_graphviz()}, an 'htmlwidget' as
returned by \code{\link[DiagrammeR:grViz]{DiagrammeR::grViz()}}.
}
\description{
\code{chaid_dot()} generates a publication-quality Graphviz DOT
description of the tree using base R only. It can be rendered with
\code{chaid_graphviz()} (which uses 'DiagrammeR', bundling viz.js so no
Graphviz binary is needed) or written to a \code{.gv} file and rendered
externally with \code{dot -Tpng} / \code{dot -Tsvg}.
}
\examples{
fit <- chaid(Species ~ ., data = iris,
             control = chaid_control(min_parent = 30, min_child = 10))
dot <- chaid_dot(fit)
}
\seealso{
\code{\link[=plot.chaid]{plot.chaid()}}, \code{\link[=chaid_plotly]{chaid_plotly()}}
}
